import React from "react";
import classes from './News.module.scss'

const News = (props) => {
    return (
        <section className={classes.section}>
            News
        </section>
    )
}

export default News